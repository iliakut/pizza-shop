import React from "react";

const OrderPage = ({cartItems, menu}) => {
  console.log(cartItems, menu)

  const menuIds = Object.keys(cartItems);

  return (
    <div className="mr-2 mt-2">
      123
    </div>
  );
};

export default OrderPage;
